useForm is a hook from the *react-hook-form* library. It manages your form for you - tracking what the user typed, running validation, and knowing when the form is submitting. 

When you call it, it returns an object with different properties. 